# Build Demo Docs
#
# Flow:
#
#   push → main / pull_request → main
#     │
#     ├─ build-portal ──┐
#     ├─ build-sphinx ──┤
#     └─ build-mkdocs ──┤
#                       ▼
#                    deploy (always runs — assembles _site/ for PR validation)
#                      ├─ download portal, sphinx, mkdocs artifacts
#                      ├─ assemble _site/{sphinx,mkdocs} + portal at root
#                      ├─ upload combined-site artifact (for inspection)
#                      ├─ [if push] actions/configure-pages
#                      ├─ actions/upload-pages-artifact
#                      └─ [if push] actions/deploy-pages
#
#   pages: write and id-token: write are scoped to the deploy job only —
#   pull requests keep the minimal contents: read permission.

name: Build Demo Docs

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Minimal default; deploy job overrides with pages + id-token.
permissions:
  contents: read

env:
  PYTHON_DEFAULT: "3.13"

jobs:
  build-portal:
    name: Portal (MkDocs)
    runs-on: ubuntu-latest

    steps:
      - name: 🔀 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_DEFAULT }}

      - name: 📦 Install portal dependencies
        run: pip install -r docs/requirements.txt

      - name: 📄 Copy README and assets into portal docs
        run: |
          cp README.md docs/docs/index.md
          cp -r assets docs/docs/assets

      - name: 📖 Build portal
        working-directory: docs
        run: mkdocs build --strict

      - name: 📤 Upload portal artifact
        uses: actions/upload-artifact@v4
        with:
          name: portal-docs
          path: docs/site/
          retention-days: 7

  build-sphinx:
    name: Sphinx (RST)
    runs-on: ubuntu-latest

    steps:
      - name: 🔀 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_DEFAULT }}

      - name: 📦 Install package + Sphinx dependencies
        run: pip install -e . -r temp_docs_sphinx/requirements.txt

      - name: 📖 Build Sphinx documentation
        working-directory: temp_docs_sphinx
        run: make html

      - name: 📤 Upload Sphinx docs artifact
        uses: actions/upload-artifact@v4
        with:
          name: sphinx-docs
          path: temp_docs_sphinx/build/html/
          retention-days: 7

  build-mkdocs:
    name: MkDocs (Markdown)
    runs-on: ubuntu-latest

    steps:
      - name: 🔀 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_DEFAULT }}

      - name: 📦 Install package + MkDocs dependencies
        run: pip install -e . -r temp_docs_mkdocs/requirements.txt

      - name: 📖 Build MkDocs documentation
        working-directory: temp_docs_mkdocs
        run: mkdocs build --strict

      - name: 📤 Upload MkDocs docs artifact
        uses: actions/upload-artifact@v4
        with:
          name: mkdocs-docs
          path: temp_docs_mkdocs/site/
          retention-days: 7

  deploy:
    name: Assemble & Deploy to GitHub Pages
    needs: [build-portal, build-sphinx, build-mkdocs]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}

    steps:
      - name: 📥 Download portal artifact
        uses: actions/download-artifact@v4
        with:
          name: portal-docs
          path: _site

      - name: 📥 Download Sphinx artifact
        uses: actions/download-artifact@v4
        with:
          name: sphinx-docs
          path: _site/sphinx

      - name: 📥 Download MkDocs artifact
        uses: actions/download-artifact@v4
        with:
          name: mkdocs-docs
          path: _site/mkdocs

      - name: ⚙️ Configure GitHub Pages
        if: github.event_name == 'push'
        uses: actions/configure-pages@v5

      - name: 📤 Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site/

      - name: 🚀 Deploy to GitHub Pages
        if: github.event_name == 'push'
        id: deploy
        uses: actions/deploy-pages@v4
